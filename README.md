# test-data-generation-validation
A Python-based tool that generates and validates valid, invalid, and boundary test data from user-defined field rules, with AI used only for validation-result explanations.
